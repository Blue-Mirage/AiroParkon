/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  darkMode: 'class', // Enables dark mode via 'dark' class
  theme: {
    extend: {}, // Extend colors/shapes here if needed
  },
  plugins: [],
};